# VCS / CI
.git
.gitignore
.github

# Docker
Dockerfile
.dockerignore

# Local runtime artifacts (never bake these into the image)
*.db
*.db-shm
*.db-wal
config.yaml
/gateway
/modelcheck
/dist

# Node / portal build artifacts — the image's ui stage installs and builds these fresh.
ui/node_modules
ui/dist
internal/portal/dist

# Docs / editor cruft
*.md
.vscode
.idea
